import { ArrowUpRight, Mail, MapPin, Phone } from 'lucide-react'
import { readContent } from './store'

export default function DynamicFooter() {
  const content=readContent(); const links=content.nav.filter(item=>item.enabled).sort((a,b)=>a.order-b.order); const plans=content.plans.filter(item=>item.enabled).sort((a,b)=>a.order-b.order).slice(0,4)
  return <footer className="footer"><div className="shell"><div className="footer-cta"><div><p className="eyebrow">READY WHEN YOU ARE</p><h2>Let’s protect what<br/><em>matters most.</em></h2></div><a href="#contact" className="footer-cta-button">Get a free quote <ArrowUpRight size={17}/></a></div><div className="footer-grid"><div className="footer-brand"><a className="brand" href="#top"><img className="site-logo" src={content.logo} alt={content.siteName}/></a><p>{content.footer.description}</p><div className="socials"><a href={`tel:${content.contact.phone.replace(/[^0-9+]/g,'')}`}><Phone size={17}/></a><a href={`mailto:${content.contact.email}`}><Mail size={17}/></a><a href="#contact"><MapPin size={17}/></a></div></div><div className="footer-col"><h4>Explore</h4>{links.map(link=><a href={link.href} key={link.id}>{link.label}</a>)}</div><div className="footer-col"><h4>Insurance</h4>{plans.map(plan=><a href="#insurance" key={plan.id}>{plan.title}</a>)}</div><div className="footer-col"><h4>Contact</h4><a href="#about">About us</a><a href="#services">Services</a><a href="#contact">Contact enquiries</a><a href={`tel:${content.contact.phone.replace(/[^0-9+]/g,'')}`}>{content.contact.phone}</a></div></div><div className="footer-bottom"><span>{content.footer.copyright}</span><span>{content.contact.hours}</span></div></div></footer>
}
